name: Version bump

# Raises the version on a dev -> master pull request so that merging it
# publishes a release. release.yml only publishes when a manifest version
# differs from what is live on npm, PyPI and crates.io; nothing else computes
# that difference, so without this a merge is a silent no-op.
#
# The bump lands on dev, not on master after the merge. Bumping on master
# strands dev at the old version, so the next cycle either conflicts across all
# five manifests or presents a diff that lowers the version.

on:
  pull_request:
    types: [opened, synchronize, reopened]

permissions:
  contents: read

concurrency:
  group: version-bump-${{ github.event.pull_request.number }}
  cancel-in-progress: true

jobs:
  bump:
    name: Bump version for release
    if: >-
      github.repository == 'GagaKor/rtsp-backchannel' &&
      github.event.pull_request.head.repo.full_name == github.repository &&
      github.base_ref == 'master' &&
      github.head_ref == 'dev'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      contents: write
    steps:
      # The head branch itself, not the merge commit: the bump is pushed back to
      # dev. Full depth because the bump reads every commit since the last tag.
      - uses: actions/checkout@v7
        with:
          ref: ${{ github.head_ref }}
          fetch-depth: 0

      # last_released() resolves the previous release by tag existence, not by
      # reachability -- release.yml tags the dev -> master merge commit, which
      # is never an ancestor of dev. So the check here is that this checkout
      # actually fetched the tags, not that any of them is reachable.
      # The bare assignment is deliberate: a failing command substitution
      # inside a larger argument does not trip `set -e`.
      - name: Require tag history
        run: |
          set -euo pipefail
          tags=$(git tag --list 'v*')
          if [ -z "$tags" ]; then
            echo 'No v* tags in this checkout; the bump cannot resolve the last release.' >&2
            exit 1
          fi
          echo "Release tags present: $(printf '%s' "$tags" | tr '\n' ' ')"

      - uses: actions/setup-python@v7
        with:
          python-version: '3.11'

      - name: Compute and apply the version bump
        id: bump
        run: |
          set -euo pipefail
          summary=$(python tools/bump_version.py --root .)
          echo "$summary"
          echo "bumped=$(printf '%s' "$summary" | jq -r '.bumped')" >> "$GITHUB_OUTPUT"
          echo "version=$(printf '%s' "$summary" | jq -r '.version')" >> "$GITHUB_OUTPUT"
          printf '%s\n' "$summary" >> "$GITHUB_STEP_SUMMARY"

      # A GITHUB_TOKEN push starts no workflow run, and when GitHub does create
      # one it can park it behind an approval, so the pull request's own checks
      # cannot be relied on to re-run against the bump commit. This job is
      # therefore the only place the bumped tree is actually tested before it
      # reaches master, where release.yml runs the same suites immediately
      # before publishing -- and a suite that fails there publishes some
      # registries and not others.
      #
      # Both suites that read release-managed files run here. The TypeScript
      # one is not optional: src/index.test.ts reads CHANGELOG.md, package.json
      # and package-lock.json, and its engines guard broke on the very first
      # automated bump. Python's suite asserts the version in pyproject.toml and
      # round-trips the real CHANGELOG.md.
      #
      # Rust is deliberately left to CI. Nothing under rust/ asserts
      # CARGO_PKG_VERSION or reads CHANGELOG.md, so a version-only rewrite
      # cannot break it, and Cargo.toml/Cargo.lock agreement is already checked
      # structurally by the bump script's own gate. Running it here would add
      # about ninety seconds of toolchain and build for no coverage.
      - name: Verify the bumped tree (Python)
        if: steps.bump.outputs.bumped == 'true'
        run: PYTHONPATH=python:. python -m unittest discover -s python -p 'test_*.py'

      - uses: actions/setup-node@v7
        if: steps.bump.outputs.bumped == 'true'
        with:
          node-version: 22
          cache: npm

      - name: Verify the bumped tree (TypeScript)
        if: steps.bump.outputs.bumped == 'true'
        run: |
          set -euo pipefail
          npm ci
          npm test
          npm run typecheck

      - name: Commit and push the bump
        if: steps.bump.outputs.bumped == 'true'
        run: |
          set -euo pipefail
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add \
            package.json \
            package-lock.json \
            python/pyproject.toml \
            python/test_library_api.py \
            rust/Cargo.toml \
            rust/Cargo.lock \
            CHANGELOG.md \
            README.md \
            README.ko.md \
            python/README.md \
            python/README.ko.md \
            rust/README.md \
            rust/README.ko.md
          if git diff --cached --quiet; then
            echo 'Version already at ${{ steps.bump.outputs.version }}; nothing to commit.'
            exit 0
          fi
          git commit -m 'chore(release): bump version to ${{ steps.bump.outputs.version }}'
          git push origin "HEAD:${{ github.head_ref }}"
